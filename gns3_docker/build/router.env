#Vaciamos tabla de enrutamiento y creamos rutas en el archivo /etc/routes
PACKS="vim curl iputils-ping net-tools nano wget procps iptables traceroute dnsutils"
COMMANDS="DEBIAN_FRONTEND=noninteractive apt install -y iptables-persistent"

